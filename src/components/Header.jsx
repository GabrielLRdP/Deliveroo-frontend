import logo from "../assets/images/logo-teal.svg";

const Header = () => {
  return (
    <header>
      <div className="container">
        <img src={logo} alt="" />
      </div>
    </header>
  );
};

export default Header;
